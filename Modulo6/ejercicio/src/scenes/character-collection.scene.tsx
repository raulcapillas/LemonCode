import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharactersCollectionContainer } from 'pods';

export const CharacterCollectionScene = () => (
  <AppLayout>
    <CharactersCollectionContainer/>
  </AppLayout>
);
